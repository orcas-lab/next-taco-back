import { WsExceptionFilter } from './ws-exception-filter.filter';

describe('WsExceptionFilterFilter', () => {
    it('should be defined', () => {
        expect(new WsExceptionFilter()).toBeDefined();
    });
});
